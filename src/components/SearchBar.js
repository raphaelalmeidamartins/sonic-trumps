import React from 'react';

function SearchBar() {
  return (
    <section>
      SearchBar
      {/* the user should be able to search for cards in the deck page */}
    </section>
  );
}

export default SearchBar;
